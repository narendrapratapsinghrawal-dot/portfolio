# Portfolio Project

### How to run this project:
1. Open this folder in VS Code.
2. Open a new terminal in VS Code and run `npm install`
3. Run `npm run dev` to start the live server and view the website.
